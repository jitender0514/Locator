{
  "name": "employee-locator",
  "version": "1.0.0",
  "description": "Employee Locator Web App",
  "scripts": {
    "start": "live-server --port=3000 --host=localhost"
  },
  "author": “employee-locator",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
